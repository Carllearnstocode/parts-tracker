/* ==========================================================================
   PitStop — Welcome screen init
   "Enter the Garage" now navigates directly to garage.html (see index.html),
   so there's no click handler needed here anymore. Kept as a home for any
   future welcome-screen-only behavior (e.g. live stats in the eyebrow line
   once vehicles/parts exist).
   ========================================================================== */
